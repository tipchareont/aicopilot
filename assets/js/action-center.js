'use strict';
(() => {
  const $ = (id) => document.getElementById(id);
  const clean = (value) => String(value ?? '').trim();
  const upper = (value) => clean(value).toUpperCase();
  const esc = (value) => clean(value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
  const num = (value, digits = 0) => Number(value || 0).toLocaleString('th-TH',{minimumFractionDigits:digits,maximumFractionDigits:digits});
  const money = (value) => `฿${num(value,2)}`;
  const dateText = (value) => { const v=clean(value).slice(0,10); if(!v)return '-'; const [y,m,d]=v.split('-'); return y&&m&&d?`${d}/${m}/${y}`:v; };
  const state = { data:null, selected:null, tab:'decisions', loading:false };
  const CACHE_VERSION = 1;
  const cacheKey = () => `ai_marketing_copilot_action_center_v1_${clean(localStorage.getItem('username')||'user').toLowerCase()}`;
  const todayBangkok = () => new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Bangkok',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());

  function readCache(){try{const x=JSON.parse(localStorage.getItem(cacheKey())||'null');return Number(x?.version||0)===CACHE_VERSION&&x?.data?x:null}catch{return null}}
  function saveCache(data){try{localStorage.setItem(cacheKey(),JSON.stringify({version:CACHE_VERSION,saved_at:Date.now(),data}))}catch{}}
  function showApp(){ $('loading').classList.add('hidden'); $('shell').classList.remove('hidden'); }
  function setUser(){ $('displayName').textContent=localStorage.getItem('display_name')||localStorage.getItem('username')||'-'; $('role').textContent=localStorage.getItem('role')||'-'; $('actionOwner').value=localStorage.getItem('display_name')||localStorage.getItem('username')||''; }

  async function request(payload){
    const controller=new AbortController(); const timeout=setTimeout(()=>controller.abort(),45000);
    try{
      const response=await fetch(window.APP_CONFIG.ACTION_MANAGEMENT_URL,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({session_token:window.Auth.token(),...payload}),signal:controller.signal});
      let data=null; try{data=await response.json()}catch{}
      if(response.status===401){window.Auth.redirectToLogin();throw new Error('Session หมดอายุ')}
      if(!response.ok||data?.success===false)throw new Error(data?.message||`Action API Error (${response.status})`);
      return data;
    }catch(error){if(error?.name==='AbortError')throw new Error('Action API ใช้เวลาตอบนานเกิน 45 วินาที');throw error}finally{clearTimeout(timeout)}
  }

  function uniqueOptions(id, rows, getter){const s=$(id),current=s.value,values=[...new Set(rows.map(getter).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'th'));s.innerHTML='<option value="">ทั้งหมด</option>'+values.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('');s.value=values.includes(current)?current:'';}
  function statusClass(value){const v=upper(value);if(v==='CRITICAL'||v==='WORSE')return 'critical';if(v==='WATCH'||v==='PENDING'||v==='INCONCLUSIVE')return 'watch';if(v==='IMPROVED')return 'improved';if(v==='STABLE')return 'stable';return v.toLowerCase().replace(/_/g,'-');}
  function similarCases(cases){if(!Array.isArray(cases)||!cases.length)return '';return `<details class="similar-box"><summary>🧠 เคสคล้ายกัน ${cases.length} เคส</summary><div class="similar-list">${cases.map(c=>`<div class="similar-case"><strong>${esc(c.Action_Type)} · ${esc(c.Outcome_Status)}</strong>${esc(c.Action_Detail||'-')}<br/>7D: ${esc(c.Baseline_Value)} → ${esc(c.Review_7D_Value)} (${num(c.Review_7D_Change_Pct,2)}%)</div>`).join('')}</div></details>`;}
  function decisionCard(row){const status=upper(row.Issue_Status),active=row.has_active_action===true;return `<article class="decision-card is-${status.toLowerCase()}"><div class="decision-head"><div class="decision-title"><h3>${esc(row.Entity_Name||'-')}</h3><p>${esc(row.Game_Name||row.Game_ID)} · ${esc(row.Account_Name||row.Account_ID)} · ${esc(row.Objective)} · ${esc(row.Phase)}</p></div><div class="badge-row"><span class="issue-badge ${statusClass(status)}">${esc(status)}</span><span class="issue-badge watch">Priority ${num(row.Priority_Score)}</span>${active?'<span class="action-status">มี Action แล้ว</span>':''}</div></div><div class="decision-reason"><strong>ปัญหาที่พบ</strong><br/>${esc(row.Issue_Reason||'-')}</div><div class="decision-recommendation"><strong>AI Recommendation</strong>${esc(row.AI_Recommendation||'-')}</div>${similarCases(row.similar_cases)}<div class="decision-actions"><button class="button-primary" data-record="${esc(row.Decision_Key)}" ${(!row.can_manage||active)?'disabled':''} type="button">${active?'บันทึกแล้ว':'บันทึก Action'}</button><button class="button-danger-ghost" data-dismiss="${esc(row.Decision_Key)}" ${(!row.can_manage||active)?'disabled':''} type="button">Dismiss</button></div></article>`;}
  function checkpoint(label,date,value,change,status){return `<div class="checkpoint"><span>${label}</span><strong>${date?`${dateText(date)} · ${value!==''&&value!=null?num(value,2):'-'}`:'รอข้อมูล'}</strong>${status?`<span class="outcome-badge ${statusClass(status)}">${esc(status)}${change!==''&&change!=null?` ${num(change,2)}%`:''}</span>`:''}</div>`;}
  function actionCard(row){const status=upper(row.Action_Status),outcome=upper(row.Outcome_Status||'PENDING');return `<article class="action-card"><div class="action-head"><div class="action-title"><h3>${esc(row.Entity_Name||'-')}</h3><p>${esc(row.Action_Type)} · Owner: ${esc(row.Owner||'-')} · เริ่ม ${dateText(row.Action_Date)}</p></div><div class="badge-row"><span class="action-status ${statusClass(status)}">${esc(status)}</span><span class="outcome-badge ${statusClass(outcome)}">${esc(outcome)}</span></div></div><div class="action-detail"><strong>สิ่งที่ทำ:</strong> ${esc(row.Action_Detail||'-')}<br/><strong>Baseline:</strong> ${esc(row.Baseline_Metric||'-')} ${row.Baseline_Value!==''?num(row.Baseline_Value,2):'-'} · Spend ${money(row.Baseline_Spend||0)} · Results ${num(row.Baseline_Results||0)}</div><div class="action-progress">${checkpoint('1 Day',row.Review_1D_Date,row.Review_1D_Value,row.Review_1D_Change_Pct,row.Review_1D_Status)}${checkpoint('3 Days',row.Review_3D_Date,row.Review_3D_Value,row.Review_3D_Change_Pct,row.Review_3D_Status)}${checkpoint('7 Days',row.Review_7D_Date,row.Review_7D_Value,row.Review_7D_Change_Pct,row.Review_7D_Status)}${checkpoint('Outcome',row.Review_7D_Date,row.Review_7D_Value,row.Review_7D_Change_Pct,row.Outcome_Status)}</div>${row.Outcome_Reason?`<div class="decision-reason"><strong>Outcome:</strong><br/>${esc(row.Outcome_Reason)}</div>`:''}<div class="action-meta"><span>${esc(row.Game_Name||row.Game_ID)}</span><span>${esc(row.Account_Name||row.Account_ID)}</span><span>${esc(row.Objective)}</span><span>${esc(row.Issue_Type)}</span></div><div class="action-actions">${['PLANNED'].includes(status)?`<button class="button-primary" data-update="${esc(row.Action_ID)}" data-status="IN_PROGRESS" type="button">เริ่มดำเนินการ</button>`:''}${['OUTCOME_READY'].includes(status)?`<button class="button-primary" data-update="${esc(row.Action_ID)}" data-status="CLOSED" type="button">ตรวจแล้วและปิด Action</button>`:''}${!['CLOSED','CANCELLED','DISMISSED'].includes(status)?`<button class="button-secondary" data-update="${esc(row.Action_ID)}" data-status="CANCELLED" type="button">ยกเลิก</button>`:''}</div></article>`;}
  function patternCard(row){return `<article class="pattern-card"><strong>${esc(row.Action_Type)} · ${esc(row.Issue_Type)}</strong><span>${esc(row.Objective)} · Sample ${num(row.Sample_Size)}</span><span>Improved ${num(row.Improved_Count)} · Success ${num(row.Success_Rate_Pct,1)}%</span><span>Average Change ${num(row.Average_Change_Pct,2)}%</span></article>`;}

  function filteredDecisions(){const q=upper($('searchFilter').value),g=$('gameFilter').value,a=$('accountFilter').value,s=upper($('statusFilter').value);return (state.data?.decisions||[]).filter(r=>(!q||upper(`${r.Entity_Name} ${r.Issue_Reason}`).includes(q))&&(!g||(r.Game_Name||r.Game_ID)===g)&&(!a||(r.Account_Name||r.Account_ID)===a)&&(!s||upper(r.Issue_Status)===s));}
  function filteredActions(){const q=upper($('searchFilter').value),g=$('gameFilter').value,a=$('accountFilter').value,s=upper($('statusFilter').value);return (state.data?.actions||[]).filter(r=>(!q||upper(`${r.Entity_Name} ${r.Action_Detail}`).includes(q))&&(!g||(r.Game_Name||r.Game_ID)===g)&&(!a||(r.Account_Name||r.Account_ID)===a)&&(!s||upper(r.Action_Status)===s||upper(r.Outcome_Status)===s));}

  function render(){const d=state.data||{},summary=d.summary||{};$('needsActionCount').textContent=num(summary.needs_action);$('inProgressCount').textContent=num(summary.in_progress);$('outcomeReadyCount').textContent=num(summary.outcome_ready);$('improvedCount').textContent=num(summary.improved);$('totalActionCount').textContent=num(summary.total_actions);$('dataDateBadge').textContent=`Data: ${d.data_date||'-'}`;const decisions=filteredDecisions(),actions=filteredActions(),patterns=d.learning_patterns||[];$('decisionResultBadge').textContent=`${num(decisions.length)} issues`;$('actionResultBadge').textContent=`${num(actions.length)} actions`;$('patternResultBadge').textContent=`${num(patterns.length)} patterns`;$('decisionList').innerHTML=decisions.length?decisions.map(decisionCard).join(''):'<div class="empty-state">ไม่มีปัญหาที่ตรงกับตัวกรอง</div>';$('actionList').innerHTML=actions.length?actions.map(actionCard).join(''):'<div class="empty-state">ยังไม่มี Action ที่บันทึกไว้</div>';$('patternGrid').innerHTML=patterns.length?patterns.map(patternCard).join(''):'<div class="empty-state">ยังไม่มี Action ที่ครบ Review 7 วัน</div>';bindDynamic();}
  function bindDynamic(){document.querySelectorAll('[data-record]').forEach(b=>b.addEventListener('click',()=>selectDecision(b.dataset.record)));document.querySelectorAll('[data-dismiss]').forEach(b=>b.addEventListener('click',()=>dismissDecision(b.dataset.dismiss)));document.querySelectorAll('[data-update]').forEach(b=>b.addEventListener('click',()=>updateAction(b.dataset.update,b.dataset.status)));}
  function selectDecision(key){const row=(state.data?.decisions||[]).find(r=>r.Decision_Key===key);if(!row)return;state.selected=row;$('selectedIssue').innerHTML=`<strong>${esc(row.Entity_Name)}</strong><p>${esc(row.Issue_Status)} · ${esc(row.Issue_Reason)}</p>`;$('actionDate').value=todayBangkok();$('saveActionButton').disabled=!row.can_manage;$('actionMessage').textContent='';window.scrollTo({top:document.body.scrollHeight/4,behavior:'smooth'});}
  function clearSelection(){state.selected=null;$('selectedIssue').innerHTML='<strong>ยังไม่ได้เลือกปัญหา</strong><p>กด “บันทึก Action” จากรายการด้านซ้าย</p>';$('actionForm').reset();$('actionOwner').value=localStorage.getItem('display_name')||localStorage.getItem('username')||'';$('saveActionButton').disabled=true;$('actionMessage').textContent='';}
  function applyMutation(row){if(!row)return;const actions=[...(state.data.actions||[])];const idx=actions.findIndex(x=>x.Action_ID===row.Action_ID);if(idx>=0)actions[idx]=row;else actions.unshift(row);state.data.actions=actions;if(row.Action_Status==='DISMISSED')state.data.decisions=(state.data.decisions||[]).filter(d=>d.Decision_Key!==row.Decision_Key);else state.data.decisions=(state.data.decisions||[]).map(d=>d.Decision_Key===row.Decision_Key?{...d,has_active_action:true}:d);const s=state.data.summary||{};s.total_actions=actions.length;s.needs_action=(state.data.decisions||[]).filter(d=>!d.has_active_action).length;s.in_progress=actions.filter(a=>['IN_PROGRESS','PENDING_REVIEW'].includes(upper(a.Action_Status))).length;s.outcome_ready=actions.filter(a=>upper(a.Action_Status)==='OUTCOME_READY').length;s.improved=actions.filter(a=>upper(a.Outcome_Status)==='IMPROVED').length;state.data.summary=s;saveCache(state.data);render();}

  async function createAction(event){event.preventDefault();if(!state.selected)return;const btn=$('saveActionButton');btn.disabled=true;$('actionMessage').className='form-message';$('actionMessage').textContent='กำลังบันทึก Action และ Baseline...';try{const r=await request({action:'CREATE_ACTION',decision_key:state.selected.Decision_Key,game_id:state.selected.Game_ID,account_id:state.selected.Account_ID,action_type:$('actionType').value,action_detail:$('actionDetail').value,action_date:$('actionDate').value,owner:$('actionOwner').value,expected_result:$('expectedResult').value,user_note:$('userNote').value});applyMutation(r.action);$('actionMessage').className='form-message success';$('actionMessage').textContent='บันทึก Action เรียบร้อย ระบบจะ Review ผลที่ 1 / 3 / 7 วัน';clearSelection();}catch(e){$('actionMessage').className='form-message error';$('actionMessage').textContent=e.message;btn.disabled=false;}}
  async function dismissDecision(key){const row=(state.data?.decisions||[]).find(d=>d.Decision_Key===key);if(!row||!confirm(`Dismiss ปัญหา “${row.Entity_Name}” ใช่หรือไม่?`))return;try{const r=await request({action:'DISMISS_DECISION',decision_key:key,game_id:row.Game_ID,account_id:row.Account_ID,dismiss_reason:'ทีมตรวจสอบแล้วและเลือกไม่ดำเนินการในรอบนี้'});applyMutation(r.action);}catch(e){alert(e.message)}}
  async function updateAction(id,status){if(!confirm(`เปลี่ยนสถานะ Action เป็น ${status} ใช่หรือไม่?`))return;try{const r=await request({action:'UPDATE_ACTION',action_id:id,action_status:status});applyMutation(r.action);}catch(e){alert(e.message)}}

  function setTab(tab){state.tab=tab;document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));$('decisionsView').classList.toggle('hidden',tab!=='decisions');$('actionsView').classList.toggle('hidden',tab!=='actions');$('learningView').classList.toggle('hidden',tab!=='learning');}
  async function load(refresh=false){if(state.loading)return;state.loading=true;$('loadingMessage').textContent=refresh?'กำลังรีเฟรช Action Center...':'กำลังโหลด Action Center...';try{if(!refresh){const cached=readCache();if(cached?.data){state.data=cached.data;$('cacheUpdatedAt').textContent=`Local cache: ${new Date(cached.saved_at).toLocaleString('th-TH')}`;prepareFilters();render();setUser();showApp();state.loading=false;return;}}const data=await request({action:'GET_ACTION_CENTER'});state.data=data;saveCache(data);$('cacheUpdatedAt').textContent=`Updated: ${new Date(data.generated_at).toLocaleString('th-TH')}`;prepareFilters();render();setUser();showApp();}catch(e){$('loadingMessage').textContent=e.message||'โหลด Action Center ไม่สำเร็จ';}finally{state.loading=false}}
  function prepareFilters(){const rows=[...(state.data?.decisions||[]),...(state.data?.actions||[])];uniqueOptions('gameFilter',rows,r=>clean(r.Game_Name||r.Game_ID));uniqueOptions('accountFilter',rows,r=>clean(r.Account_Name||r.Account_ID));}

  $('actionForm').addEventListener('submit',createAction);$('clearSelectionButton').addEventListener('click',clearSelection);$('refreshButton').addEventListener('click',()=>load(true));$('logoutButton').addEventListener('click',()=>window.Auth.redirectToLogin());$('resetFiltersButton').addEventListener('click',()=>{$('searchFilter').value='';$('gameFilter').value='';$('accountFilter').value='';$('statusFilter').value='';render();});['searchFilter','gameFilter','accountFilter','statusFilter'].forEach(id=>$(id).addEventListener(id==='searchFilter'?'input':'change',render));document.querySelectorAll('[data-tab]').forEach(b=>b.addEventListener('click',()=>setTab(b.dataset.tab)));
  $('actionDate').value=todayBangkok();setTab('decisions');load(false);
})();
